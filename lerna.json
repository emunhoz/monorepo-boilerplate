{
  "npmClient": "yarn",
  "useWorkspaces": true,
  "version": "independent",
  "command": {
    "version": {
      "allowBranch": "main",
      "ignoreChanges": ["**/*.md"]
    }
  },
  "packages": [
    "packages/*"
  ]
}
